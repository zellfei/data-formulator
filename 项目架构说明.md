# Data Formulator 项目架构说明

## 项目概述

**Data Formulator** 是微软研究院开发的一个AI驱动的数据可视化探索工具原型。它允许分析师通过AI代理迭代地探索和可视化数据，结合用户界面交互（UI）和自然语言（NL）输入来表达意图、控制探索方向并创建报告。

### 核心特性
- 🔌 **多数据源支持**: 支持CSV、Excel、数据库(MySQL、PostgreSQL、BigQuery、MongoDB等)、截图和文本解析
- 🤖 **AI代理驱动**: 使用大语言模型(LLM)进行数据转换、可视化推荐和自动探索
- 📊 **丰富的可视化**: 支持多种图表类型(条形图、散点图、折线图、地图等)
- ✅ **可验证性**: 用户可以检查生成的数据、公式、解释和代码
- 📝 **报告生成**: 基于选定图表创建和分享报告

## 技术栈

### 前端 (Frontend)
- **框架**: React 18 + TypeScript
- **状态管理**: Redux Toolkit (with Redux Persist)
- **UI库**: Material-UI (MUI)
- **可视化**: Vega, Vega-Lite, D3.js
- **拖拽**: react-dnd
- **构建工具**: Vite
- **样式**: SCSS + Emotion

### 后端 (Backend)
- **框架**: Flask (Python)
- **数据处理**: Pandas, DuckDB
- **AI集成**: LiteLLM (支持OpenAI、Azure、Anthropic、Ollama等)
- **数据库**: DuckDB (本地), 支持外部数据库连接
- **数据加载**: 多种数据源连接器

## 项目结构

```
data-formulator/
├── src/                          # 前端源代码
│   ├── app/                      # Redux store和应用配置
│   ├── components/               # 可复用组件
│   ├── views/                    # 页面视图组件
│   ├── data/                     # 数据类型和工具
│   └── scss/                     # 样式文件
├── py-src/data_formulator/       # 后端源代码
│   ├── agents/                   # AI代理实现
│   ├── workflows/                # 工作流编排
│   ├── data_loader/              # 数据加载器
│   ├── security/                 # 安全验证
│   ├── app.py                    # Flask应用主入口
│   ├── agent_routes.py           # AI代理路由
│   ├── tables_routes.py          # 数据表路由
│   └── db_manager.py             # 数据库管理
├── public/                       # 静态资源
├── package.json                  # Node.js依赖
├── pyproject.toml                # Python包配置
└── README.md                     # 项目说明
```

## 核心文件详解

### 1. 前端核心文件

#### `/src/app/App.tsx`
**作用**: 应用主组件和顶层布局
- 管理应用的顶层状态和路由
- 提供全局UI元素(工具栏、模型选择、数据加载对话框等)
- 处理主题配置
- 定义路由结构(主编辑器、关于页面等)

**关键功能**:
```typescript
// 主要负责:
- 应用路由配置
- 全局工具栏和菜单
- 模型配置对话框
- 数据加载入口
- 会话管理
```

#### `/src/app/dfSlice.tsx`
**作用**: Redux状态管理的核心切片
- 定义整个应用的状态结构(`DataFormulatorState`)
- 包含所有Redux actions和reducers
- 管理数据表、图表、模型配置、消息等状态

**关键数据结构**:
```typescript
interface DataFormulatorState {
    sessionId: string;              // 会话ID
    models: ModelConfig[];          // AI模型配置
    tables: DictTable[];            // 数据表
    charts: Chart[];                // 图表
    conceptShelfItems: FieldItem[]; // 概念架上的字段
    messages: Message[];            // 消息日志
    agentActions: AgentAction[];    // AI代理操作
    generatedReports: Report[];     // 生成的报告
    // ... 更多状态
}
```

#### `/src/views/DataFormulator.tsx`
**作用**: 主数据探索界面
- 实现数据探索的主界面布局
- 集成数据视图、可视化视图、概念架等组件
- 处理数据加载、示例会话加载
- 提供模型测试和配置功能

**核心组件集成**:
```typescript
// 主要包含:
- DataView: 数据表视图
- VisualizationView: 可视化图表视图
- ConceptShelf: 字段概念架
- DataThread: 数据探索线程
- ReportView: 报告视图
```

#### `/src/views/VisualizationView.tsx`
**作用**: 图表可视化视图
- 渲染Vega-Lite图表
- 提供图表交互和编码控制
- 支持拖拽式图表编辑
- 处理图表与数据的绑定
- 实现图表保存、导出等功能

**关键功能**:
- 图表渲染和更新
- 编码通道(x, y, color, size等)管理
- AI辅助的数据转换和图表生成
- 图表模板选择

#### `/src/views/DataView.tsx`
**作用**: 数据表格视图
- 展示和编辑数据表
- 支持数据排序、过滤
- 显示派生字段和数据转换
- 提供数据锚定功能

#### `/src/components/ComponentType.tsx`
**作用**: TypeScript类型定义
- 定义所有核心数据结构
- 图表(Chart)、编码(EncodingMap)、字段(FieldItem)等类型
- 数据表(DictTable)结构
- 确保类型安全

**核心类型**:
```typescript
interface Chart {
    id: string;
    chartType: string;
    encodingMap: EncodingMap;
    tableRef: string;
    // ...
}

interface DictTable {
    id: string;
    name: string;
    rows: any[];
    schema: FieldItem[];
    // ...
}
```

### 2. 后端核心文件

#### `/py-src/data_formulator/app.py`
**作用**: Flask应用主入口
- 初始化Flask应用
- 注册蓝图(blueprints)路由
- 配置环境变量和API密钥
- 提供静态文件服务
- 管理会话和服务器配置

**关键路由注册**:
```python
# 注册蓝图
app.register_blueprint(tables_bp)  # 数据表相关API
app.register_blueprint(agent_bp)   # AI代理相关API
```

#### `/py-src/data_formulator/agent_routes.py`
**作用**: AI代理相关的API路由
- 定义所有与AI代理交互的HTTP端点
- 处理数据转换请求
- 处理可视化推荐请求
- 处理探索流程请求
- 管理流式响应(SSE)

**主要端点**:
```python
# 关键API路由:
/api/agents/transform           # 数据转换
/api/agents/recommend           # 图表推荐
/api/agents/explore             # 自动探索
/api/agents/report              # 报告生成
/api/agents/clean               # 数据清洗
/api/agents/derive-concept      # 概念派生
```

#### `/py-src/data_formulator/tables_routes.py`
**作用**: 数据表相关的API路由
- 处理数据表的CRUD操作
- 管理数据库连接和查询
- 处理数据上传和导入
- 提供SQL查询执行接口

**主要功能**:
```python
# 数据表操作:
/api/tables/upload              # 上传数据文件
/api/tables/query               # 执行SQL查询
/api/tables/list                # 列出所有表
/api/tables/info                # 获取表信息
```

#### `/py-src/data_formulator/db_manager.py`
**作用**: 数据库管理器
- 管理DuckDB数据库连接
- 提供表的创建、查询、删除操作
- 处理大数据集的分页加载
- 管理会话级数据库实例

**核心功能**:
```python
class DatabaseManager:
    def create_table()      # 创建表
    def execute_query()     # 执行查询
    def get_table_info()    # 获取表信息
    def register_dataframe() # 注册Pandas DataFrame
```

### 3. AI代理文件

#### `/py-src/data_formulator/agents/agent_py_data_transform.py`
**作用**: Python数据转换代理
- 使用AI生成Python代码进行数据转换
- 处理用户的自然语言数据转换请求
- 执行生成的代码并返回结果
- 支持代码修复和错误处理

**工作流程**:
1. 接收用户的转换意图(自然语言)
2. 分析输入数据和目标图表编码
3. 生成Python/Pandas代码
4. 执行代码并验证结果
5. 返回转换后的数据

#### `/py-src/data_formulator/agents/agent_sql_data_transform.py`
**作用**: SQL数据转换代理
- 使用AI生成SQL查询进行数据转换
- 适用于大数据集(使用DuckDB)
- 优化查询性能
- 处理复杂的聚合和连接操作

#### `/py-src/data_formulator/agents/agent_py_data_rec.py`
**作用**: Python可视化推荐代理
- 根据数据特征推荐合适的可视化
- 生成多个可视化建议
- 评估可视化的有效性
- 提供推荐理由和洞察

#### `/py-src/data_formulator/agents/agent_exploration.py`
**作用**: 自动探索代理
- 实现高层次的自动化数据探索
- 生成探索计划
- 协调多个转换和可视化步骤
- 创建探索报告

#### `/py-src/data_formulator/agents/agent_report_gen.py`
**作用**: 报告生成代理
- 基于选定的图表生成报告
- 使用AI撰写洞察和解释
- 支持多种报告风格
- 生成Markdown格式输出

#### `/py-src/data_formulator/agents/agent_data_clean.py`
**作用**: 数据清洗代理
- 识别数据质量问题
- 生成清洗建议
- 执行数据清洗操作
- 处理缺失值、异常值等

#### `/py-src/data_formulator/agents/client_utils.py`
**作用**: LLM客户端工具
- 封装与各种LLM服务的交互
- 处理API调用和错误重试
- 支持流式响应
- 统一不同提供商的接口

**关键类**:
```python
class Client:
    def call_llm()          # 调用LLM
    def call_llm_stream()   # 流式调用
    def parse_response()    # 解析响应
```

### 4. 工作流文件

#### `/py-src/data_formulator/workflows/exploration_flow.py`
**作用**: 探索工作流编排
- 实现完整的自动探索流程
- 协调规划、转换、可视化等步骤
- 管理多轮迭代
- 处理错误和回退

**流程步骤**:
```python
def run_exploration_flow_streaming():
    # 1. 接收高层次目标
    # 2. 生成探索计划
    # 3. 执行数据转换
    # 4. 创建可视化
    # 5. 迭代优化
    # 6. 生成最终报告
```

#### `/py-src/data_formulator/workflows/create_vl_plots.py`
**作用**: Vega-Lite图表创建
- 从数据生成Vega-Lite规范
- 处理各种图表类型配置
- 进行数据类型推断
- 生成图表预览图像

**关键函数**:
```python
def assemble_vegailte_chart()  # 组装Vega-Lite规范
def detect_field_type()         # 检测字段类型
def spec_to_base64()           # 转换为图像
```

### 5. 数据加载器

#### `/py-src/data_formulator/data_loader/`
**作用**: 多数据源连接器
- 提供统一的数据加载接口
- 支持各种数据源:
  - MySQL (`mysql_data_loader.py`)
  - PostgreSQL (`postgresql_data_loader.py`)
  - BigQuery (`bigquery_data_loader.py`)
  - MongoDB (`mongodb_data_loader.py`)
  - Azure Data Explorer
  - S3和Azure Blob存储

**基础类结构**:
```python
class BaseDataLoader:
    def connect()       # 建立连接
    def load_data()     # 加载数据
    def list_tables()   # 列出表
    def disconnect()    # 断开连接
```

### 6. 安全模块

#### `/py-src/data_formulator/security/query_validator.py`
**作用**: SQL查询安全验证
- 验证SQL查询的安全性
- 防止SQL注入
- 限制危险操作(DROP, DELETE等)
- 确保查询符合安全策略

## 核心流程和数据流

### 1. 应用启动流程

```
用户启动应用
    ↓
python -m data_formulator (py-src/data_formulator/__main__.py)
    ↓
run_app() (py-src/data_formulator/app.py)
    ↓
Flask服务器启动 (后端API)
    ↓
浏览器打开 http://localhost:5000
    ↓
加载React应用 (src/index.tsx → src/app/App.tsx)
    ↓
初始化Redux Store (src/app/store.ts)
    ↓
渲染DataFormulator组件 (src/views/DataFormulator.tsx)
```

### 2. 数据加载流程

```
用户点击"加载数据"
    ↓
打开数据加载对话框 (DataLoadingChatDialog)
    ↓
选择数据源:
  - 上传文件 (CSV/Excel) → /api/tables/upload
  - 连接数据库 → /api/tables/connect
  - 解析截图/文本 → /api/agents/extract
    ↓
后端处理数据
    ↓
创建DuckDB表 (db_manager.py)
    ↓
返回表信息到前端
    ↓
更新Redux state (tables数组)
    ↓
显示在数据视图中 (DataView.tsx)
```

### 3. AI辅助可视化创建流程

```
用户拖拽字段到编码架或输入自然语言
    ↓
触发Formulate按钮
    ↓
发送请求到 /api/agents/transform
    ↓
agent_routes.py 路由处理
    ↓
选择代理 (Python或SQL):
  - 小数据: agent_py_data_transform.py
  - 大数据: agent_sql_data_transform.py
    ↓
AI代理分析:
  1. 理解用户意图
  2. 分析输入数据
  3. 生成转换代码
    ↓
执行代码 (py_sandbox.py)
    ↓
验证结果
    ↓
创建Vega-Lite图表规范 (create_vl_plots.py)
    ↓
返回到前端:
  - 转换后的数据
  - 图表规范
  - 代码和解释
    ↓
更新Redux (添加新表和图表)
    ↓
渲染可视化 (VisualizationView.tsx)
```

### 4. 自动探索流程

```
用户输入探索目标并启用Agent模式
    ↓
发送请求到 /api/agents/explore (流式响应)
    ↓
exploration_flow.py 处理
    ↓
ExplorationAgent 生成探索计划
    ↓
迭代执行步骤:
  第1步: 数据转换 → PythonDataRecAgent / SQLDataRecAgent
  第2步: 创建可视化
  第3步: 评估结果
  第4步: 决定下一步 (继续/分支/完成)
    ↓
每步通过SSE流式返回进度
    ↓
前端实时更新:
  - 显示进度消息
  - 添加新图表
  - 创建数据线程分支
    ↓
完成后生成探索报告
```

### 5. 报告生成流程

```
用户选择图表并点击"生成报告"
    ↓
发送请求到 /api/agents/report
    ↓
agent_report_gen.py 处理
    ↓
收集选定图表的:
  - 数据摘要
  - 图表类型
  - 关键洞察
    ↓
AI生成报告内容:
  - 标题和摘要
  - 各图表的解释
  - 数据驱动的洞察
  - 结论和建议
    ↓
返回Markdown格式报告
    ↓
前端显示 (ReportView.tsx)
    ↓
支持编辑和导出
```

## 关键架构节点

### 1. Redux Store (src/app/store.ts + dfSlice.tsx)
**职责**: 全局状态管理中心
- 管理所有应用状态
- 提供状态持久化(redux-persist)
- 协调UI组件和后端数据

**核心状态分类**:
- **数据状态**: tables, charts
- **UI状态**: focusedTableId, focusedChartId, viewMode
- **配置状态**: models, selectedModelId, config
- **会话状态**: sessionId, messages, agentActions

### 2. Flask Blueprint 路由系统
**职责**: API端点组织和请求分发
- `tables_bp`: 数据表操作API
- `agent_bp`: AI代理操作API

**优势**:
- 模块化路由管理
- 清晰的职责分离
- 易于扩展和维护

### 3. AI代理生态系统 (agents/)
**职责**: AI能力的模块化实现
- 各代理专注特定任务
- 可组合和协调
- 统一的LLM客户端接口

**代理类型**:
- **数据代理**: 转换、清洗、派生
- **可视化代理**: 推荐、创建
- **探索代理**: 规划、执行
- **辅助代理**: 报告、解释

### 4. DuckDB数据库管理器 (db_manager.py)
**职责**: 高性能数据存储和查询
- 会话级数据库实例
- 大数据集支持
- SQL查询优化
- 与Pandas无缝集成

**优势**:
- 内存高效
- 快速分析查询
- 支持标准SQL
- 零配置

### 5. 组件通信层
**职责**: 前后端数据交互

**通信模式**:
- **RESTful API**: 标准HTTP请求/响应
- **SSE (Server-Sent Events)**: 流式数据推送
- **JSON**: 数据序列化格式

**关键端点**:
```
数据操作:
  POST /api/tables/upload
  POST /api/tables/query

AI代理:
  POST /api/agents/transform
  POST /api/agents/recommend
  POST /api/agents/explore (SSE)
  POST /api/agents/report
```

### 6. 可视化引擎 (Vega-Lite + React-Vega)
**职责**: 声明式图表渲染
- 基于JSON的图表规范
- 丰富的图表类型
- 交互式可视化
- 响应式设计

**流程**:
```
数据 → Vega-Lite规范 → React-Vega组件 → SVG/Canvas渲染
```

## 数据模型

### 1. 表 (DictTable)
```typescript
interface DictTable {
    id: string;              // 唯一标识符
    name: string;            // 表名
    rows: any[];             // 数据行
    schema: FieldItem[];     // 字段模式
    parent?: string;         // 父表ID (派生表)
    description?: string;    // 描述
    code?: string;          // 生成代码
    sql?: string;           // SQL查询
    anchored?: boolean;     // 是否锚定
    inDB?: boolean;         // 是否在数据库中
}
```

### 2. 图表 (Chart)
```typescript
interface Chart {
    id: string;              // 唯一标识符
    chartType: string;       // 图表类型 (bar, point, line等)
    encodingMap: EncodingMap; // 编码映射 (x, y, color等)
    tableRef: string;        // 关联的表ID
    vegaLiteSpec?: object;   // Vega-Lite规范
    saved: boolean;          // 是否保存
    source: string;          // 来源 (user/trigger)
}
```

### 3. 字段 (FieldItem)
```typescript
interface FieldItem {
    name: string;            // 字段名
    type: Type;              // 数据类型
    description?: string;    // 描述
    derived?: boolean;       // 是否派生
    formula?: string;        // 计算公式
}
```

## 扩展和自定义

### 添加新的数据源连接器
1. 在`py-src/data_formulator/data_loader/`创建新文件
2. 继承`BaseDataLoader`类
3. 实现`connect()`, `load_data()`, `list_tables()`等方法
4. 在UI中添加对应的配置表单

### 添加新的图表类型
1. 在`src/components/ChartTemplates.tsx`定义新模板
2. 配置默认编码通道
3. 在`create_vl_plots.py`添加Vega-Lite规范生成逻辑
4. 更新UI选择器

### 添加新的AI代理
1. 在`py-src/data_formulator/agents/`创建新代理文件
2. 定义代理的提示词和逻辑
3. 在`agent_routes.py`注册新端点
4. 在前端添加触发按钮

### 自定义代理规则
- 在UI中打开"Agent Rules"对话框
- 编辑探索规则或编码规则
- 这些规则会注入到AI代理的提示词中

## 性能优化

### 前端优化
- **Code Splitting**: Vite自动进行代码分割
- **懒加载**: 按需加载大型组件
- **Memoization**: 使用React.memo避免不必要的重渲染
- **Virtual Scrolling**: 大型列表使用虚拟滚动

### 后端优化
- **DuckDB**: 高效的列式存储和查询
- **流式响应**: SSE避免长时间等待
- **缓存**: LRU缓存常用查询结果
- **异步处理**: 后台任务队列

### AI优化
- **提示词工程**: 精心设计的提示词提高准确性
- **增量修复**: 错误代码自动修复
- **上下文管理**: 控制上下文长度减少token使用

## 安全考虑

### 代码执行安全
- **沙箱环境**: Python代码在受限环境中执行
- **超时控制**: 防止无限循环
- **资源限制**: 限制内存和CPU使用

### SQL安全
- **查询验证**: 防止SQL注入
- **操作限制**: 禁止DROP、DELETE等危险操作
- **会话隔离**: 每个用户独立的数据库会话

### API安全
- **密钥管理**: 支持环境变量存储API密钥
- **可选显示**: 可配置隐藏密钥显示
- **CORS配置**: 跨域请求控制

## 总结

Data Formulator是一个架构清晰、模块化良好的数据可视化工具:

**核心优势**:
1. **前后端分离**: React前端 + Flask后端，职责清晰
2. **AI驱动**: 多个专业AI代理协同工作
3. **可扩展性**: 易于添加新数据源、图表类型、AI代理
4. **高性能**: DuckDB支持大数据集，SSE提供流式体验
5. **用户友好**: UI + NL混合交互模式，降低使用门槛

**关键创新点**:
- AI代理的模块化设计
- 数据探索的线程化管理
- 声明式可视化规范
- 混合交互模式(UI + NL)

这个架构使得Data Formulator既能满足专业分析师的需求，又能让普通用户快速上手进行数据探索。
